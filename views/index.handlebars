
<div class="container">
  <div class="row">
<h1>burgers!</h1>
  </div>
<div class="row">
<img src="../assets/img/borger.png">
</div>
<div class="row">
<h2>these are burgers you can eat!!!!</h2>
</div>
<div class="row">
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="row">
<h2>these are burgers that you can regurgitate so you can eat them AGAIN!!!</h2>
</div>

<div class="row">
<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>

<h2>Add a Burger</h2>
<div class="row">
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name: </label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">are you going to eat that or?</label><br>
    <input type="radio" name="devoured" value="1" checked> CONSUME IMMEDIATELY<br>
    <input type="radio" name="devoured" value="0"> DON'T EAT YET
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>
</div>